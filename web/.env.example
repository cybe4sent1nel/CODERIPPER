# =========================================
# CodeRipper Environment Configuration
# =========================================
# Copy this file to .env.local and fill in your values
# For Vercel deployment, add these as Environment Variables in Project Settings

# =========================================
# Application Configuration
# =========================================
# Your deployed app URL (update for production)
# For local development:
NEXT_PUBLIC_APP_URL=http://localhost:3000
# For production (Vercel):
# NEXT_PUBLIC_APP_URL=https://coderipper.vercel.app

# For local development with exec-engine
NEXT_PUBLIC_API_URL=http://localhost:8081

# Environment mode
NODE_ENV=development

# =========================================
# AI Model Configuration (OpenRouter)
# =========================================
# Get your API key from https://openrouter.ai/keys
# REQUIRED for AI features to work
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Primary AI Model (used first)
# Free models: nvidia/llama-3.1-nemotron-70b-instruct:free, meta-llama/llama-3-8b-instruct:free
# Paid models: openai/gpt-4o-mini, anthropic/claude-3-haiku, google/gemini-pro
AI_MODEL_PRIMARY=nvidia/llama-3.1-nemotron-70b-instruct:free

# Fallback Models (if primary fails)
AI_MODEL_FALLBACK_1=meta-llama/llama-3-8b-instruct:free
AI_MODEL_FALLBACK_2=google/gemini-2.0-flash-exp:free
AI_MODEL_FALLBACK_3=mistralai/mistral-7b-instruct:free

# AI Configuration
AI_MAX_TOKENS=2000
AI_TEMPERATURE=0.7
AI_TIMEOUT_MS=30000
AI_MAX_RETRIES=3

# =========================================
# Code Execution Configuration
# =========================================
# For Vercel deployment, use Piston public API (no setup needed)
# The app automatically uses https://emkc.org/api/v2/piston when EXEC_ENGINE_URL is not set

# Optional: Your own Piston instance URL
# EXEC_ENGINE_URL=https://your-piston-instance.com

# Execution limits
EXEC_TIMEOUT_SECONDS=30
EXEC_MEMORY_LIMIT_MB=256

# =========================================
# Optional: Database Configuration
# =========================================
# For persistent leaderboard/user data (not required for basic functionality)
# DATABASE_URL=postgresql://user:password@host:5432/coderipper
# MONGODB_URL=mongodb://localhost:27017/coderipper
# REDIS_URL=redis://localhost:6379

# =========================================
# Optional: Analytics
# =========================================
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# =========================================
# Vercel-Specific Notes
# =========================================
# 1. In Vercel Dashboard → Project Settings → Environment Variables:
#    - Add OPENROUTER_API_KEY (required for AI features)
#    - Add NEXT_PUBLIC_APP_URL with your production URL
#    - All other variables have sensible defaults
#
# 2. The app uses Piston public API for code execution by default
#    No additional setup needed for code running feature
#
# 3. Leaderboard data is stored in-memory by default
#    For persistence, connect a database (optional)
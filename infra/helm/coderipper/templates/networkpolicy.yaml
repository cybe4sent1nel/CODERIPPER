apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-egress-from-runner
spec:
  podSelector:
    matchLabels:
      app: coderipper-runner
  policyTypes:
  - Egress
  egress: []

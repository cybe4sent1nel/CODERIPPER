apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc

# Note: Ensure your cluster has gVisor installed and a RuntimeClass named 'gvisor' configured. For Kata, set handler to 'kata-runtime'.
